services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - MAX_CONCURRENT_JOBS=${MAX_CONCURRENT_JOBS:-1}
    ulimits:
      nofile:
        soft: 65535
        hard: 65535
      nproc: 4096
    restart: unless-stopped
